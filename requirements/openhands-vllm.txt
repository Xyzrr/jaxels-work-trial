# Canonical vLLM runtime for scripts/run_openhands_swebench_eval_pod.sh.
#
# Keep this pinned. The eval launcher resolves this top-level input into a
# pod-local lock, syncs /workspace/venvs/openhands-vllm from that resolved file,
# and verifies it with pip check before serving.
vllm==0.9.2

# vLLM 0.9.2 imports its own Ovis/AIMv2 config. Newer Transformers releases
# register aimv2 themselves, which makes the vLLM CLI fail at import time.
transformers==4.53.3
